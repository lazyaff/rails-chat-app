development:
    adapter: async

test:
    adapter: test

production:
    adapter: redis
    url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
    channel_prefix: chat_app_production
    cable:
        url: wss://rails-chat-app-production.up.railway.app/cable
